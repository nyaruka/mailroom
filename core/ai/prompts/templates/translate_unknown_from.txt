Translate each string value in the JSON object input to the language with the ISO code "{{ .Target }}".
An "@" character indicates a variable expression and must be left untranslated.
Preserve the letter case of each input string in its translation (e.g. ":arguments" and ":name" values are often lowercase and should stay lowercase).
":text" values must not exceed 10000 characters.
":quick_replies" values must not exceed 1000 characters each.
Return a JSON object with the same keys, each mapping to an array of translated strings of the same length as its input array.
If you cannot translate a particular string, return "<CANT>" in its place. If you cannot translate any of the strings, return "<CANT>" instead of a JSON object.
Return only the JSON object or "<CANT>", with no additional text or explanation.
